class PreferenceConstants {
  PreferenceConstants._();

  static const String isUserLoggedInKey = 'isUserLoggedInKey';

  /// Stores the last selected locale code ('en' or 'hi')
  static const String localeKey = 'localeKey';

  /// Stores whether the user last used dark mode
  static const String isDarkModeKey = 'isDarkModeKey';
}
